// Gasmoji Config FIle
// Woof better figure this out or hes gonna be sleeping outside

// import config file
const settings = require('./settings');

// combine the config to object
module.exports = {
    settings: settings
};