// # Tools
// nodejs module for support libs for the application

// defines that javascript should be executed in 'strict mode' (ECMAScript 5)
'use strict';

// import dependencies
const logger = require("../logger")("tools");
const Promise = require("bluebird");
const fs = require('fs');

// # Tools
class Tools {
    
};

module.exports = Tools;